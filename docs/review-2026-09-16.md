# Review working tree, 16 September 2026

Scope: repair the changes already present; do not resume feature development or deploy.

Historical review checkpoint. The user subsequently authorized completing missing features; see [visual-sku-workflow.md](visual-sku-workflow.md) for the later implementation and activation steps.

## Repairs

- Connect the existing object-selection editor to Vite and the canonical multi-file input. Each photo submits its own normalized crop and selection source. Pointer interaction supports touch; edge dragging/resizing stays inside the image. Late automatic proposals cannot overwrite manual edits.
- Remove the upload dependency on the missing `ProductImages` service. Store full original catalog files, with crop metadata kept separately. Preserve the legacy embedding flow; the opt-in FAISS flow uses reference export/build.
- Add explicit retrieval configuration with `legacy` as the default and feedback disabled by default. Feedback is staged only when enabled and requested by an authenticated user. Failed cache writes clean up pending photos; expired pending photos have a scheduled cleanup command.
- Resolve catalog image URLs through the configured disk, with `public` compatibility for older records. Delete associated variants with the selected photo.
- Prevent a failed object proposal from running a second, inconsistent detector. Explicit full-image reference selections remain full-image selections.
- Invalidate incompatible indexes/policies after preprocessing changes. A frozen policy cannot be applied when a required model is unavailable or category filtering changes the candidate pipeline.
- Sweep calibration score boundaries in O(C log C), replacing repeated full candidate scans. Reject duplicate identities/SKUs and already-filtered tuning input. Held-out evaluation may contain only negative queries.

## Operational boundaries

No production deployment, push, model replacement, threshold activation, or real-data accuracy claim is part of this review. No application database migration was executed.

The new additive `2026_09_16_000000_index_reference_metadata.php` migration adds disk metadata and lookup indexes. Existing production migrations were not edited. Apply pending migrations only as part of a separately reviewed deployment.

Existing FAISS generations must be rebuilt before using the changed preprocessing. Frozen relevance policies must be recalibrated against the matching pipeline. The default legacy path is unchanged. Feedback remains disabled until its complete flow is validated.

Automated tests cover application integration, calibration invariants, inference fallbacks, and canvas geometry. They do not substitute for mobile browser acceptance testing, model accuracy evaluation, or a 100,000-reference load test.

Validation: Laravel 28 passed (111 assertions); Python 35 passed, 1 optional real-model test skipped; frontend 3 passed; `npm run build` succeeded. Vite reported the optional `fontaine` font-fallback dependency warning; it did not block the build.
